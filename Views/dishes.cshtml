<h1>All dishes</h1>
@if (@Model.Count == 0)
{
  <p>There are no dishes.</p>
}
<ul>
  @foreach (var dish in Model)
  {
  <li><a href="/stores/@dish.GetId()">@dish.GetId()</a></li>
    <li>@dish.GetName()</li>
    <li>@dish.GetPrice()</li>
    <li>@dish.GetStoreId()</li>

  }
</ul>
<a href="/dishes/new">Add new dish</a>
